# Local credentials — never publish these
postgis.cfg

# OS / editor noise
.DS_Store
*.swp

# Python bytecode
__pycache__/
*.pyc

# Benchmark data. None of it belongs in a code release: the CSV export is
# regenerated by database/export_csv.py, the SpatiaLite database by
# spatialite/load.py, and data_txt/ is a third-party product distributed by
# the Nevada Geodetic Laboratory, which should be cited rather than mirrored.
scripts/benchmark/data/
scripts/benchmark/data_txt/
scripts/benchmark/spatialite/*.sqlite
